# Cemu fork is tracked in its own repo (see Cemu/FORK.md)
Cemu/

# CMake build output (disposable, regenerable)
build/

# Third-party research checkouts (pins in AI_HARNESS_INSTRUCTIONS.md)
references/

# Gradle caches and build outputs
.gradle-home/
**/.gradle/
**/build/
captures/
.cxx/
.externalNativeBuild/
*.apk
*.aab

# Machine-specific files and secrets
local.properties
*.jks
*.keystore
*.pem
*.key
keys.txt
CemuPad-*.zip
dist/

# Session screenshots and diagnostic logs (AI-generated, kept on disk only;
# anchored so source assets like vanilla launcher icons stay tracked)
/*.png
/*.log
/*.ps1
/android-gamepad-app/*.png

# IDE and editor state
.idea/
*.iml
.vs/
.vscode/

# OS artifacts
.DS_Store
Thumbs.db
